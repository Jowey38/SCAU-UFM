[project]
name = "scau-ufm-numerical-sandbox"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "sympy",
  "notebook",
]

[tool.scau-ufm]
runner = "python sandbox/numerical/src/run_golden_sandbox.py"
tests = "python -m unittest discover -s sandbox/numerical/tests -p test_*.py"
